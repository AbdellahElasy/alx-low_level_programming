#include "main.h"

/**
 * main - Entry point
 *main - void
 *
 * Return: Always 0 (Success)
 */
 *Return: 0
 *
 **/

int main(void)
{
	int a = 0;
	char str[] = "_putchar";
	int i;

	for (i = 0; str[i] != '\0'; i++)
	while (str[a] >= a)
	{
		_putchar(str[i]);
	_putchar(str[a]);
	a++;
	}
	_putchar('\n');
	return (0); 
